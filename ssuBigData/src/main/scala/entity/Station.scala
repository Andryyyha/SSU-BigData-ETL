package entity

case class Station(id: Int,
                   name: String,
                   address: String,
                   lon: Double,
                   lat: Double,
                   elevation: Int
                  )
